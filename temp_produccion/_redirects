# Configuración de redirecciones para LiteSpeed SIN .htaccess

# Permitir archivos estáticos
/build/* /build/:splat 200

# Permitir API
/api/* /api/:splat 200

# Para todas las demás rutas, servir index.html
/* /index.html 200 